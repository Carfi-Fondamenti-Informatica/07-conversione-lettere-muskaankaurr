#include <iostream>
#include "lib.h"
using namespace std;

int main(){
  char a;
  cin >> a;
  bool carattere (char &a);
  if(carattere (a) ){
    cout << a << endl;
  } else {
    cout << "errore" << endl;
 }    
  return 0;
}
